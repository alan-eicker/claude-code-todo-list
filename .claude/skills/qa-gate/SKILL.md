---
name: qa-gate
description: Run all four completion gates (lint, tsc, jest, playwright) in sequence and fix any failures
---

---

name: qa-gate
description: Run all four completion gates (lint, tsc, jest, playwright) in sequence and fix any failures

---

# QA Gate

This skill is now a project rule at `@.claude/rules/qa-gate.md`. Follow that rule file for mandatory completion criteria.
